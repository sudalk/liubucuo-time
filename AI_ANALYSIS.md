# 柳不匆 AI 分析方案

> 版本：v1.0
> 日期：2026-07-11
> 关联：`TECH.md` 第 4.4 节、`柳不匆-产品需求方案-讨论稿.md` 第 9 章

## 1. 设计目标

让用户在日、周、月三个时间维度上都能获得 AI 辅助的复盘能力。每个维度提供「关键词 + 总结 + 行动建议」三层输出，全部可编辑、可重新生成、不静默覆盖用户编辑。

## 2. 三维度分析矩阵

| 维度 | 输入数据 | 关键词 | 总结 | 行动建议 |
|---|---|---|---|---|
| **日** | 当日时间记录 + 状态 + 感悟 | 1-5 个关键词 | — | — |
| **周** | 一周记录聚合 + 日关键词 + 感悟 | — | 事实摘要 + 观察 | ≤3 条行动建议 |
| **月** | 当月记录聚合 + 周总结 + 感悟 | 月关键词（聚合日关键词） | 月度摘要 + 主要变化 | ≤3 条行动建议 |

每个维度的 AI 输出都落库到 `summaries` 表（`kind` 字段区分 `daily`/`weekly`/`monthly`），关联 `user_edited` 字段：AI 重新生成时不静默覆盖用户编辑稿。

## 3. API 设计

三个端点，统一风格：

```
POST /api/ai/analyze
  body: { period: "daily" | "weekly" | "monthly", date?: string }
  response: {
    keywords: string[],           // 日/月关键词（周维度可空）
    summary: string,              // 周/月总结（日维度可空）
    observations: string[],       // 观察（周/月）
    actions: string[],            // 行动建议（周/月）
    changes: string[],            // 主要变化（月）
    causes: string[]              // 可能原因（月）
  }
```

落库：生成后自动写入 `summaries` 表，`period` 格式：
- 日：`2026-07-11`
- 周：`2026-W28`
- 月：`2026-07`

## 4. Prompt 规则

所有 prompt 遵守产品文档第 9.6 条 AI 体验原则：
- 首次启用时说明使用哪些数据及用途
- 区分「事实统计」「模型推断」「行动建议」
- 不静默覆盖用户编辑内容
- 不输出医疗诊断或高风险建议
- 数据不足时明确说明，不强行下结论

## 5. 前端交互

分析页顶部三档切换：日 / 周 / 月。切换后：
1. 默认展示当前区间已有的统计（时间分布、状态平均）
2. 若已有 AI 输出（`summaries` 表里），直接展示，标注「AI 生成 · 可编辑」
3. 点「重新生成」按钮触发 AI，生成后写入 DB
4. 用户编辑总结文本后失焦保存，`user_edited=true`，后续 AI 生成前需确认覆盖

## 6. 与原计划的差异

原 `TECH.md` 把 AI 列为「日关键词 + 周总结 + 周期分析」三件套。本方案调整为：
- 日维度：关键词（保留）
- 周维度：总结 + 观察 + 行动建议（合并周期分析到周维度）
- 月维度：月关键词（聚合日关键词）+ 月度总结 + 主要变化 + 行动建议（新增）

理由：用户更需要「这周怎么样」「这个月趋势如何」，而不是「本周 vs 上周」的纯对比。对比作为月维度的一部分。
